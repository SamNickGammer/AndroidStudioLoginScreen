<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demoapplication01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoApplication01">
        <activity android:name=".AdminSignUp"></activity>
        <activity android:name=".WelcomeAdmin" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".WelcomeUser" />
        <activity android:name=".BasicCalculator" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SignIn"
            android:clearTaskOnLaunch="true"
            android:label="@string/title_activity_sign_in"
            android:theme="@style/Theme.DemoApplication01.NoActionBar" />
        <activity android:name=".LoginScreen">
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>